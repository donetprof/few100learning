

describe('writing specs in typescript', () => {
    it('is easy', () => {
        expect(true).toBe(true);
    });
    it('counting numbers', () => {
        const name = 'Ramesh';
        expect(name.length).toBe(6);
    });
});

describe('write more tests', () => {
    it('add two numbers', () => {
        expect(2 + 3).toBe(5);
    });

});
